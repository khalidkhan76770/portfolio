- name: Setup Node
  uses: actions/setup-node@v4
  with:
    node-version: 18
    cache: 'npm'

- name: Install
  run: npm install

- name: Build
  run: npm run build

- name: Export static site
  run: npx next export -o out
